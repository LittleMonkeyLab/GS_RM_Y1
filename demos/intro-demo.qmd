---
title: "Introduction to R (Quarto Live)"
format: live-html
engine: knitr
webr:
  packages: [ggplot2, dplyr]
---

{{< include ../_extensions/r-wasm/live/_knitr.qmd >}}

# 🧠 Welcome to R!

R is a programming language for data analysis, statistics, and visualization. Let's try it out!

## 📦 Getting Started

We can use R just like a calculator:

````{webr}
2 + 3
7 * 8
sqrt(49)
````

## 📌 Variables

You can store values in variables using the `<-` symbol:

````{webr}
x <- 10
y <- 5
x + y
````

## 📈 Plotting

Let’s make a simple scatterplot using the built-in `mtcars` dataset:

````{webr}
library(ggplot2)

ggplot(mtcars, aes(x = hp, y = mpg)) +
  geom_point() +
  labs(title = "Horsepower vs MPG")
````

## 🧪 Try it Yourself!

Complete the code to calculate the mean of the vector `c(1, 2, 3, 4, 5)`:

````{webr}
#| exercise: ex_mean
mean(_____)
````

````{webr}
#| setup: true
#| exercise: ex_mean
# Load base R automatically (no packages needed)
````

::::: {.hint exercise="ex_mean"}
::: {.callout-tip collapse="false"}
## Hint

Use the `mean()` function on the vector `c(1, 2, 3, 4, 5)`.
:::
:::::

::: {.solution exercise="ex_mean"}
```r
mean(c(1, 2, 3, 4, 5))
```
:::

## 📊 Working with Data

We can use the `dplyr` package to manipulate datasets. For example, filter cars with more than 20 MPG:

````{webr}
library(dplyr)

mtcars |> filter(mpg > 20)
````

## 🎯 Your Turn

Use `filter()` to show only cars with `hp` greater than 100:

````{webr}
#| exercise: ex_hp
mtcars |> ______
````

````{webr}
#| setup: true
#| exercise: ex_hp
library(dplyr)
````

::::: {.hint exercise="ex_hp"}
::: {.callout-tip}
Try this structure:

```r
mtcars |> filter(hp > 100)
```
:::
:::::

::: {.solution exercise="ex_hp"}
```r
mtcars |> filter(hp > 100)
```
:::

## ✅ Summary

- Use `<-` to create variables
- Use functions like `mean()`, `sqrt()`
- Plot using `ggplot2`
- Filter data using `dplyr`

---

👋 Great job! You've just written your first R code in the browser using Quarto Live.